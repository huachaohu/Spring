server:
  port: 9003
spring:
  application:
    name: nacos-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.64.102:80
management:
  endpoints:
    web:
      exposure:
        include: "*"

service-url:
  nacos-provider: http://nacos-provider